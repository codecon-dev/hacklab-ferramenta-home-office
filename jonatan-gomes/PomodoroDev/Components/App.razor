<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <ResourcePreloader />
    <link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["PomodoroDev.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="tomate.png" />
    <HeadOutlet />

    <style>
        :root{
            --accent: #9b6cff;
            --text:   #e6eef8;

            /* menu grafite fosco */
            --menu-dark-1: #2b2e2f;
            --menu-dark-2: #1f2223;

            /* topo PomodoroDev (preto fosco) */
            --topbar: #0b0b0b;
        }

        html, body {
            height: 100%;
            margin: 0;
            color: var(--text);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;

            /* Degrade difuso + formas coloridas (estilo da imagem anexada) */
            background-color: #1b1430;
            background-image:
                radial-gradient( at 10% 75%, rgba(255,50,200,0.95) 0%, rgba(255,50,200,0.55) 8%, rgba(255,50,200,0.0) 30% ),
                radial-gradient( at 85% 10%, rgba(0,200,255,0.95) 0%, rgba(0,200,255,0.5) 8%, rgba(0,200,255,0.0) 30% ),
                radial-gradient( at 50% 35%, rgba(110,15,130,0.95) 0%, rgba(110,15,130,0.55) 12%, rgba(110,15,130,0.0) 40% ),
                linear-gradient(135deg, rgba(38,18,86,0.95) 0%, rgba(26,18,60,0.85) 40%, rgba(10,20,60,0.95) 100%);
            background-blend-mode: screen, screen, screen, normal;
            background-size: cover;
            background-attachment: fixed;
        }

        /* leve desfoque visual central para suavizar transições (vinheta invertida) */
        body::before {
            content: "";
            position: fixed;
            inset: 0;
            pointer-events: none;
            background: radial-gradient(60% 50% at 60% 35%, rgba(255,255,255,0.02) 0%, rgba(0,0,0,0.28) 80%);
            mix-blend-mode: multiply;
        }

        /* Menu lateral: grafite cinza escuro fosco */
        .nav-scrollable {
            background: linear-gradient(180deg, var(--menu-dark-1) 0%, var(--menu-dark-2) 100%);
            border-right: 1px solid rgba(0,0,0,0.35);
            box-shadow: 2px 0 20px rgba(0,0,0,0.6);
            min-height: 100vh;
            width: 260px;
        }

        /* Top bar (PomodoroDev) em preto fosco */
        .top-row.ps-3.navbar.navbar-dark {
            background: var(--topbar);
            border-bottom: 1px solid rgba(255,255,255,0.03);
            height: 56px;
            display: flex;
            align-items: center;
            box-shadow: none;
        }

        .navbar-brand {
            color: #e9e9e9 !important;
            font-weight: 700;
            padding-left: 1rem;
            display: inline-block;
            letter-spacing: 0.2px;
        }

        /* Links do menu: contraste e estilo fosco */
        .nav-link {
            color: rgba(230,230,230,0.95);
            transition: color .12s ease, background .12s ease, transform .08s ease;
            padding: .6rem .9rem;
            border-radius: 6px;
            margin: .25rem .6rem;
            display: flex;
            align-items: center;
            background: transparent;
        }

        .nav-link:hover {
            color: #fff;
            transform: translateX(3px);
            background: rgba(155,108,255,0.06);
        }

        .nav-link.active {
            color: #fff !important;
            background: rgba(155,108,255,0.12);
        }

        .nav-item .bi { opacity: .95; color: rgba(255,255,255,0.80); }

        /* Painel central legível sobre o fundo colorido */
        .pomodoro-container {
            background: rgba(255,255,255,0.98);
            color: #222;
            box-shadow: 0 12px 30px rgba(10,10,20,0.45);
        }

        /* responsividade: reduzir intensidade de alguns efeitos */
        @@media (max-width: 900px) {
            .nav-scrollable { width: 220px; }
            body {
                background-position: center;
            }
        }
    </style>
</head>

<body>
    <Routes />
    <ReconnectModal />
    <script src="@Assets["_framework/blazor.web.js"]"></script>
</body>

</html>
