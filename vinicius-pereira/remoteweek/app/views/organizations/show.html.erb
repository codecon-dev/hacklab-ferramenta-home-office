<div class="container my-5">

  <div class="card shadow-sm border-0 mb-4">
    <div class="card-body">
      <h1 class="fw-bold mb-1"><%= @organization.name %></h1>
      <p class="text-muted mb-0">
        <strong>Slug:</strong> <%= @organization.slug %>
      </p>
    </div>
  </div>

  <div class="card shadow-sm border-0 mb-4">
    <div class="card-body">

      <h3 class="fw-semibold mb-3">Membros da Organização</h3>

      <ul class="list-group list-group-flush">
        <% @users.each do |user| %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <div>
              <strong><%= user.name %></strong>  
              <span class="text-muted d-block small"><%= user.email %></span>
            </div>

            <span class="badge bg-secondary text-uppercase">
              <%= user.role || "Membro" %>
            </span>
          </li>
        <% end %>
      </ul>

    </div>
  </div>

  <div class="d-flex gap-3">
    <%= link_to "📋 Standups da Organização",
        organization_standups_path(@organization),
        class: "btn btn-dark shadow-sm" %>

    <%= link_to "📆 Relatórios Semanais",
        organization_weekly_reports_path(@organization),
        class: "btn btn-outline-secondary shadow-sm" %>
  </div>

</div>
